# Development Rules

Brownfield first。保持现有技术栈。优先小步迁移。Domain-first。API 不围绕单个页面建模。

Git 默认 `main`。提交类型：feat/fix/refactor/test/docs/perf/chore。

每个 Phase 必须记录 changed files、compatibility、tests、migration、rollback、risks。