# P10 — AI Model V2

建立 Model Registry、Baseline、Elo、Dixon-Coles、ML、LLM、Ensemble、Champion/Challenger。

每个模型记录 dataset fingerprint、feature version、training cutoff、calibration、artifact hash。

新模型必须通过统一 evaluation 才能成为 candidate/active。