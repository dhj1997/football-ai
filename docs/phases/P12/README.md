# P12 — Advanced Backtesting

支持 walk-forward、rolling window、expanding window、cross-competition、strategy backtest。

每次 backtest 固定 dataset fingerprint、time range、model version、feature version、strategy version。

输出 calibration、probability metrics、ROI（仅链路完整时）、CLV、drawdown、equity 与 uncertainty。