# P16 — Observability

监控：API latency/error、DB health、Provider freshness/error、Scheduler jobs、Prediction success/failure、Historical accumulation、Model health、Data Quality、Leakage alerts。

所有关键任务需要 correlation/job id、duration、status、error reason。

告警必须能定位到 provider、competition、fixture、model 或 job。