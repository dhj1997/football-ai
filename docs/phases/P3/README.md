# P3 — Prediction Intelligence

## Goal
建立 FeatureSnapshot → Model → Probability → Calibration → Ensemble 的版本化链路。

## Baseline
FeatureSnapshot `p3-v1`；Ensemble `p3-ensemble-v1`；Calibration `p3-temperature-v1`。

## Acceptance
Feature/model/calibration provenance 可追溯；模型失败真实记录；不伪造结果。