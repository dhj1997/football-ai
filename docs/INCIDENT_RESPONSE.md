# Incident Response

事件：Provider outage、Data corruption、Prediction failure、Leakage、DB failure、Model failure、Frontend failure。

流程：Detect → Triage → Contain → Diagnose → Recover → Verify → Postmortem。

泄漏事件必须冻结受影响链路、保留证据、标记影响范围并重新评价。