# 结算遗漏与近期亏损复盘

## 目标

修复已下注旧预测无法结算的问题，并阻止重新计算候选时绕过已冻结的 `no_bet` 数据质量闸门。基于线上真实结算数据复盘模型、市场和风控表现；不篡改历史预测或下注，仅对明确属于代码缺陷的未结记录做定向补结算。

## 阶段

1. **只读基线与设计确认（完成）**：完成代码、线上记录和近期表现取证；用户选择继续自动模拟下注。
2. **结算修复（完成）**：让已结束比赛的所有开放 `placed` 下注参与结算，同时继续结算当前预测指标。
3. **候选闸门修复（完成）**：候选生成尊重冻结决策和关键数据质量条件；已补旧预测覆盖回归测试。
4. **验证与复盘（完成）**：结算、资金、Portfolio、双模型和 P0/P1 焦点测试全部通过。
5. **部署与补结算（完成）**：已提交、推送、部署并验证；目标下注通过受保护流程补结算且流水幂等。
6. **陈旧赛果恢复设计（完成）**：采用 30 天恢复窗口、每轮最多 20 场，复用懂球帝详情和现有自动化任务；设计提交为 `8c1862d`，用户已书面确认。
7. **恢复与曲线实现（完成）**：已补恢复候选、资金曲线参数和焦点回归测试；直接相关测试 `85 passed`。
8. **部署与定向结算（完成）**：备份恢复核验、部署、同步斯图加特赛果、正式结算和幂等验证全部通过。

## 当前状态

全部阶段完成。GitHub、本地和生产均为 `b94d5117c7913c48055812a4a2a541208b4904ef`；API/Web active，公开健康 HTTP 200，MySQL 正常。

## 取证

- 生产健康检查：MySQL、API/Web 自动化均正常。
- 预测指标：128 个样本，准确率 0.4922，平均 Brier 0.6130，平均 Log Loss 1.0308。
- DeepSeek：25 个样本，准确率 0.52，6 笔下注 1 胜 5 负，已结算下注 ROI -0.8208，已实现盈亏 -569.50。
- ChatGPT：103 个样本，准确率 0.4854，35 笔下注 14 胜 21 负，已结算下注 ROI -0.0617，已实现盈亏 -192.78。
- 诺丁汉森林 vs 考文垂的下注曾因旧预测被最新预测替换而未结算；修复部署后已按真实比分结算为 `full_loss`。
- `SettlementService` 只遍历 `current_predictions_for_fixture`；该仓储方法每模型只返回最新预测。
- `BankrollService.candidate_for_prediction` 重新构造候选时只看候选 Edge/EV/数据质量/赔率时间，不强制检查已冻结 `decision.status` 与 `reason_codes`，存在绕过 `no_bet` 闸门的风险。

## 约束

- 保留现有未提交用户规划/临时文件，不做无关重构。
- 不伪造历史时间、预测或赛果；不因复盘结果倒改历史记录。
- 所有线上写入必须在部署后通过受保护流程定向执行，并验证资金流水幂等。

## 设计确认

- 用户选择继续自动模拟下注（B）。
- 推荐的最小定向修复已获口头确认，书面设计见 `docs/superpowers/specs/2026-09-20-settlement-loss-review-design.md`。

## 完成证据

- 最终提交：设计 `53fe64b`，实现 `591caab`；`origin/main` 已与本地一致。
- 生产备份：`/opt/football-ai/backups/db-20260920-201555.sql.gz`，恢复核验 `RESTORE_VERIFIED tables=41`。
- 生产三个修复文件 SHA256 与本地完全一致；服务于 2026-09-20 20:19:33 CST 进入 active。
- 目标下注 `b456ef2c-c723-4c89-8ca1-223cafd95bc2` 已结算，净亏损 `-90.97`；账本恰好一笔 `stake` 和一笔 `return`。
- 修复进程启动后新增下注为 0，未观察到新的冻结决策绕过。
- 仍有 3 笔开放单；斯图加特 vs 维京是修复前遗留冻结单，且比赛状态仍错误地停留在 `scheduled`，需作为独立赛果同步问题处理。
- 陈旧恢复已上线：斯图加特 vs 维京主记录为 `finished`、比分 3:1，旧冻结单已通过正式流程结算；当前仅剩 2 笔有效开放单。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| PowerShell 提前展开远端 `$(mktemp ...)`，版本标记校正命令未执行 | 1 | 改用单引号保护完整远端命令；生产文件未被修改 |
| 多层 SSH 引号使只读审计脚本的 ISO 时间字符串带入空格，随后 `python -c` 双引号仍被 PowerShell 拆开 | 2 | 先构造单个 SSH 远端参数再原样传入；数据库未发生写入 |
| 设计文件末尾多余空白行导致 `git diff --cached --check` 失败 | 1 | 移除末尾空白行后重新暂存检查；尚未创建提交 |
| `session-catchup.py` 输出旧会话时因 Windows GBK 无法编码 Unicode 符号而中止 | 1 | 旧会话与当前任务无关；直接读取当前计划、Git 状态和目标文件恢复上下文 |
| 首个回归测试补丁把第二个文件的定位片段放入错误文件块，整体校验失败 | 1 | 两个测试文件均未变化；拆成独立文件补丁重试 |
| 恢复边界测试将未来比赛设为 2 小时后，实际属于现有正常同步窗口 | 1 | 改为未来 5 天，使其仅验证恢复窗口不会抓取未来比赛 |
| 等待测试时把终端 `session_id` 当成工具单元 ID | 1 | 测试进程未受影响；改用 `write_stdin` 轮询原终端会话 |
| 最终 `git ls-remote` 遇到 GitHub HTTPS 连接重置 | 1 | 推送此前已成功；改用 GitHub REST 只读接口核对 `main` SHA，不重复推送 |
