# Progress

## 2026-09-20

- 完成生产健康、预测指标、模拟账户和下注记录只读检查。
- 确认结算遗漏根因：只取每模型最新预测。
- 确认候选执行存在冻结 `no_bet` 闸门绕过风险。
- 用户选择不暂停自动模拟下注，并确认最小定向修复范围。
- 完成设计文档草案；尚未编辑业务代码、部署或写入生产数据。
- 用户确认书面设计并要求实施。
- 新增结算遗漏与冻结决策闸门回归测试，下一步先验证它们在旧实现上失败。
- 红灯确认：结算场景 1 个失败，冻结决策参数化场景 9 个全部失败，均准确复现缺陷。
- 已实现按比赛读取开放下注、结算合并历史预测、候选与最终执行双层冻结决策校验。
- 首次绿灯运行中 9 个闸门场景通过；结算重复运行仅返回当前预测，修正测试以余额和流水数量验证幂等。
- 结算、资金账户和市场决策焦点套件通过：`58 passed`。
- Portfolio/双模型共享契约通过：`34 passed`；P0 通过 `9 passed`，P1 通过 `8 passed`；新增最终边界回归通过 `10 passed`；Python 编译和差异检查通过。
- 最终业务修复提交为 `591caab`，设计提交为 `53fe64b`；首次 GitHub HTTPS 推送连接重置，随后已成功推送。
- 恢复核对确认本地与 GitHub `main` 均为 `591caab`；生产 API/Web 于 20:19:33 重启，三个修复文件哈希与本地完全一致。
- 发现 `.deploy-revision` 仍为上一版本 `12704d5`；首次原子校正命令被本机 PowerShell 提前展开，远端文件未变化，准备使用完整单引号命令重试。
- 已将生产 `.deploy-revision` 原子校正为完整 SHA `591caab0509a5d03f55c133b38ab0c793351b210`。
- 首次远端只读审计脚本因 SSH 引号导致 ISO 时间字符串带入空格而退出；未执行任何数据库写入，改用 Base64 传递脚本。
- Base64 审计脚本的第二次调用仍被 PowerShell 拆开 `python -c` 引号；未执行数据库代码，下一次构造单个远端参数后传入 SSH。
- 使用单个 SSH 远端参数完成只读审计：修复进程启动后新增下注为 0；目标单为 `settled/full_loss/-90.97`，账本恰好一笔 stake 和一笔 return。
- 当前开放下注共 3 笔：两笔有效 `bet`，一笔修复前斯图加特遗留冻结单；按批准范围未取消历史开放单。
- 最新原始结算账本为 46 笔、盈亏 `-1156.13`、ROI `-26.32%`；模型样本不足且受旧执行缺陷污染，不进行权重调整。
- 公开健康再次返回 HTTP 200，MySQL 正常，API/Web active；本轮全部阶段完成。
- 用户批准继续处理历史 `scheduled` 赛果同步和资金曲线展示；开始实现前设计调查。
- 已确认复用现有五分钟懂球帝比分任务和十五分钟结算任务，不新增调度器；资金曲线问题定位为 `summary()` 漏传配置初始资金。
- 实时核验懂球帝比赛 `54577422` 已完场，斯图加特 3:1 维京；生产同时存在正确完场记录与承载旧下注的另一条 `scheduled` 记录，继续检查跨来源去重传播。
- 确认下注绑定的 `sportsdb-2594578` 已持有同一懂球帝 ID；最小修复为受限扫描过期 `scheduled` 记录并复用现有详情查询，不新增身份系统。
- 用户选择方案 A：30 天陈旧恢复窗口、每轮最多 20 场，并同时修正资金曲线初始资金展示。
- 已写入书面设计 `docs/superpowers/specs/2026-09-20-stale-fixture-recovery-design.md`，等待用户确认后再修改业务代码。
- 设计提交前检查发现并移除文件末尾多余空白行；未带入其他工作区文件。
- 设计文档已独立提交为 `8c1862d`（`docs(api): define stale fixture recovery`），等待用户书面确认。
- 用户确认书面设计并要求实施；阶段 6 完成，阶段 7 开始。
- `session-catchup.py` 因旧会话 Unicode/GBK 输出错误中止；已通过当前计划、Git 状态和目标文件直接恢复，不使用无关旧上下文。
- 首个测试补丁因跨文件定位错误被整体拒绝，未产生文件改动；改为两个独立文件补丁。
- 已新增陈旧恢复上限/边界、共享懂球帝 ID 单次查询、详情失败保留原记录，以及非默认初始资金曲线回归测试；下一步验证旧实现红灯。
- 首轮红灯为 4 个失败；恢复测试中的未来 2 小时记录属于正常窗口，已改为未来 5 天后重跑，其余三个失败准确对应目标缺陷。
- 修正夹具后的红灯仍为 4 个失败，分别命中恢复窗口、共享比赛 ID 重复查询、失败错误摘要和资金曲线初始值；开始最小实现。
- 已实现 30 天/20 场陈旧恢复、懂球帝比赛 ID 去重和资金曲线初始资金传递；四个目标回归通过（`4 passed`）。
- 相关测试继续运行；首次等待方式选错工具 ID，未重启或影响原测试进程，改用终端会话轮询。
- 懂球帝、资金、结算和自动化直接相关测试全部通过：`85 passed`；目标文件编译和 `git diff --check` 通过。
- 差异审查确认仅修改两个业务文件和两个测试文件；阶段 7 完成，阶段 8 开始。
- 实现提交 `b94d511` 和设计提交 `8c1862d` 已推送，GitHub/local `main` 完全一致。
- 生产部署前只读检查通过：远端版本 `591caab`，API/Web active，健康接口确认 MySQL 和自动化正常；开始执行备份恢复门禁部署。
- 部署门禁成功：MySQL 备份 `/opt/football-ai/backups/db-20260920-220724.sql.gz` 恢复核验 `RESTORE_VERIFIED tables=41`，应用备份为 `/opt/football-ai/backups/app-20260920-220711.tar.gz`。
- 服务器 Next 构建/TypeScript 通过，API/Web 于 22:10:56 CST 重启；API 启动等待后健康为 MySQL `ok`，Web 返回 200。
- 部署后 `dongqiudi_sync.py` 与 `bankroll.py` 的本地/生产 SHA256 完全一致；内外健康均为 HTTP 200，API/Web active。
- 健康接口显示懂球帝同步时间已推进到重启后的 22:10:16 CST；先只读核对目标比赛/下注，避免重复生产任务。
- 已将生产 `.deploy-revision` 原子校正为 `b94d5117c7913c48055812a4a2a541208b4904ef`。
- 受保护比分任务成功：`recovery_count=12`、`updated_count=13`、错误 0；目标比赛更新为 finished、斯图加特 3:1 维京。
- 正式结算将目标旧单结算为 `full_loss/-100`、执行状态 `SETTLED`，首次核对只有一笔 stake 和一笔 return；已重复运行结算准备验证幂等。
- 重复结算后目标单的 `settled_at` 和两笔流水完全不变；开放下注剩 2 笔。
- 生产 DeepSeek/ChatGPT 账户的 `initial_balance` 与资金曲线起点均为 5000，展示修复生效。
- 最终 `git ls-remote` 连接重置；此前推送已成功，改用 GitHub REST 只读确认远端 SHA。
- GitHub REST 确认 `main=b94d5117c7913c48055812a4a2a541208b4904ef`，与本地和生产版本标记一致。
- 当前两笔开放单均为有效 `bet`、无阻断原因码，且均早于本次部署；未观察到部署后新的冻结下注。
- 阶段 8 完成，全部规划阶段完成。
