# 进度记录

## 2026-09-09

- 已读取现有自动化、下注、账本和测试实现。
- 已确认首发任务未注册、自动分析默认关闭、初始资金为 1000。
- 已注册 `lineup` 任务，自动分析默认打开，加入 24/12/6/1/0.5 小时窗口标记。
- 已加入自动化固定 100 下注、5000 初始资金传入和旧账户一次性补足逻辑。
- 定向测试首轮 33 项通过；修正新增任务导致的旧任务数量断言。
- 完整 API 测试通过：260 passed，1 个既有 Starlette/httpx 弃用警告。
- 运行配置检查通过：自动分析开启，五个窗口生效，固定下注 100，初始资金 5000。
- API 实例已在 127.0.0.1:8000 监听；`/health` 返回 MySQL、自动化开启。
- 真实作业历史确认 `lineup` 与 `analysis` 均成功执行；历史累积任务仍有既有 `canonical_fixture_id` 错误。
- `/api/bankroll` 实测返回 `initial_balance=5000`、当前余额 `4472.98`、4 笔历史模拟下注。
