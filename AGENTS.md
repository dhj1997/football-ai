# Project Agent Instructions

- 禁止过度设计。优先选择满足当前需求的最小方案，避免无关重构、提前抽象和装饰性功能。
- 禁止过度测试。测试范围必须与改动风险相称，只覆盖关键行为、失败路径和已有契约。
- 所有球员名称在 API、页面和日志展示前必须通过 `to_chinese_player_name` 转为对应的中文名称；新增数据源时补充规范中文别名，禁止直接展示供应商英文名。
